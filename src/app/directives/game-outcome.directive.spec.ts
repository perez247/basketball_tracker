import { GameOutcomeDirective } from './game-outcome.directive';

describe('GameOutcomeDirective', () => {
  it('should create an instance', () => {
    const directive = new GameOutcomeDirective();
    expect(directive).toBeTruthy();
  });
});
