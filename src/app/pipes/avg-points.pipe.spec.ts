import { AvgPointsPipe } from './avg-points.pipe';

describe('AvgPointsPipe', () => {
  it('create an instance', () => {
    const pipe = new AvgPointsPipe();
    expect(pipe).toBeTruthy();
  });
});
